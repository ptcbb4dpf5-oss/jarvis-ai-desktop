# ── JARVIS v1 dependencies ─────────────────────────────────────────────
# Core UI
PyQt6>=6.6.0

# LLM
openai>=1.30.0
requests>=2.31.0        # ElevenLabs voice API + misc HTTP

# System monitoring
psutil>=5.9.0
GPUtil>=1.4.0          # optional GPU telemetry (best-effort)
# CPU/GPU core temperatures on Windows (needs LibreHardwareMonitor or
# OpenHardwareMonitor running to expose the WMI sensors).
wmi>=1.5.1 ; platform_system == "Windows"

# Mouse / keyboard / screen control
PyAutoGUI>=0.9.54
pillow>=10.0.0          # screenshots (used by PyAutoGUI)

# Windows integration (installed only on Windows)
pywin32>=306 ; platform_system == "Windows"

# Voice I/O
SpeechRecognition>=3.10.0
pyttsx3>=2.90
# PyAudio is required for microphone input. On Windows, pip installs a wheel.
PyAudio>=0.2.13

# Browser automation
playwright>=1.44.0

# Plugin hot-reload
watchdog>=4.0.0
